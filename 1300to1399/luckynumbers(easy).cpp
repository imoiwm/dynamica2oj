#include <bits/stdc++.h>

using namespace std;
using i64 = long long; 

int main() {
  string s;
  cin>>s;
  /**int fours = (s.length() + 1) / 2, sevens = (s.length() + 1) / 2;
  if (fours + sevens > s.length()) {
    for (int i = 0; i < fours; i++) {
      cout << "4";
    }
    for (int i = 0; i < sevens; i++) {
      cout << "7";
    }
    cout << '\n';
    return 0;
  }**/

  return 0;
}